for agrupamento, coluna in zip(
    agrupar_por,
    colunas_agrupamento,
):
    valor = linha[coluna]

    if agrupamento in ["ano", "mes", "rca"]:
        valor = int(valor)
    else:
        valor = str(valor)

    item[agrupamento] = valor